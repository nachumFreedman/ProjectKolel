import React, { Component } from 'react';

class TalmudDaf extends Component {
  render(){
    return (
      <div>
        N:
        based on the three parts of the route
        convert into a url from the old system
        
        load the daf
        display unsafe html?
      </div>
    );
  }
}

export default TalmudDaf;
